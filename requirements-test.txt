# Test dependencies for CI/CD pipeline
# Include all dependencies from requirements.txt plus test-specific packages

# Base dependencies
-r requirements.txt

# Testing framework
pytest>=8.0.0
pytest-asyncio>=0.26.0
pytest-cov>=6.0.0
pytest-mock>=3.14.0

# Test utilities
coverage>=7.0.0
